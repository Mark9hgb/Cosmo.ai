<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aiassistant.termux_ai_assistant">

    <!-- Termux RUN_COMMAND permission -->
    <uses-permission android:name="com.termux.permission.RUN_COMMAND" />
    
    <!-- Internet for Nvidia NIM API -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Storage for file explorer -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        android:maxSdkVersion="29" />
    
    <!-- Notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Vibrate for feedback -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:label="AI Terminal Assistant"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <!-- Termux command receiver -->
        <receiver 
            android:name=".TermuxCommandReceiver"
            android:exported="true"
            android:permission="com.termux.permission.RUN_COMMAND">
            <intent-filter>
                <action android:name="com.termux.RUN_COMMAND" />
                <action android:name="com.termux.RUN_COMMAND_GET_OUTPUT" />
            </intent-filter>
        </receiver>
        
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    
    <!-- Query to verify Termux is installed -->
    <queries>
        <package android:name="com.termux" />
        <intent>
            <action android:name="com.termux.RUN_COMMAND" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>